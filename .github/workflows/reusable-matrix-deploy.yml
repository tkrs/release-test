name: reusable-matrix-deploy

on:
  workflow_call:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    strategy:
      matrix:
        target: [a, b, c]
    steps:
      - name: Simulate deploy step
        run: |
          echo "Deploying target: ${{ matrix.target }}"
          sleep 5
          echo "Done: ${{ matrix.target }}"
